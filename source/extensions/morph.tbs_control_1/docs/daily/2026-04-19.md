## 작업 목표

- 최근 코드 수정(EP 타임라인 시계, Kit 막대 UI, 분할 이력 로그)이 문서에 누락되어 있어 **CHANGELOG·일지·관련 postmortem/가이드**에 반영한다.

---

## 변경 요약(핵심 파일/핵심 로직)

| 주제 | 파일 | 내용 |
|------|------|------|
| 막대 축 = SimPy 시각 | `morph/tbs_control_1/simulation_engine.py` | `tick()`의 `timeline_only` emit에서 `sim_time`을 `virtual_now` → **`env.now`** |
| 막대 깜빡임·분할 폭 | `morph/tbs_control_1/control_window.py` | `_update_ep_timeline_under_port_state`: dt=0 등일 때 재빌드 스킵; `_ep_occ_timeline_layout_dims`·가로 스크롤 정책 |
| 이력 화면별 분리 | `morph/tbs_control_1/control_window.py` | `_append_sim_log`: 멀티 시 **`[화면N]`을 raw에서 파싱** 후 본문만 `_format_history_line` |
| 릴리즈 요약 | `docs/CHANGELOG.md` | 위 항목을 **Unreleased** 블록으로 기록 |
| 배경 문서 | `docs/ep_timeline_progress_postmortem.md`, `docs/sim_viewport_split_multi_usd_guide.md` | 현행 정책·분할 시 모니터 유의사항 보강 |

---

## 문제/증상 (정리)

1. **막대 vs t(sim)**: `timeline_only`가 `virtual_now`를 쓰면 SimPy 이벤트 사이에서도 막대 `dt`가 쌓여 진행 패널의 시뮼 시각보다 빨리 보일 수 있음(분할에서 두드러짐).
2. **단일 화면 깜빡임**: 매 갱신마다 `ep_timeline_widget` 전체 destroy/rebuild.
3. **분할 막대 잘림**: 고정 `BAR_W=420` + 이름·우측 라벨·패딩이 열 너비를 초과; 가로 스크롤 없음.
4. **이력이 전부 화면1**: `_format_history_line` → 이모지 접두 후 `^\[화면N]` 매칭 실패 → 폴백으로 화면1만 갱신.

---

## 원인 분석

- (1) UI 막대 축과 엔진 “표시용 시뮼 시각” 소스가 **`virtual_now` vs `env.now`**로 어긋남.
- (2) 동일 프레임·동일 데이터인데도 전체 위젯 트리 재구성.
- (3) `Fraction(1.0)` 열 폭 대비 행 최소 폭이 큼.
- (4) 라우팅이 **포맷 후 문자열** 기준이라 접두가 더 이상 줄의 시작이 아님.

---

## 최종 해결

- 엔진: `timeline_only`의 `sim_time` = **`env.now`** (주석으로 의도 명시).
- Kit: 스킵 키에 레이아웃 튜플 저장; 분할별 축소 치수 + **`_ep_timeline_host_horizontal_scroll_policy`**.
- 이력: **raw strip → `[화면N]` 분리 → body만 포맷 → `_append_sim_log_channel(si, …)`**.

---

## 남은 TODO / 다음 작업

- 필요 시 **Unreleased**를 버전 번호 섹션으로 승격.
- 웹(HTTP 브리지) 쪽 이력이 화면별 스냅샷과 동일 정책인지 사용처 확인 시 한 줄 보강.
