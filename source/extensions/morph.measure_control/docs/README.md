# morph.measure_control

`morph.measure_control` scaffold extension.

This extension keeps the same high-level structure as `morph.section_control`
(`extension.py`, `service.py`, `core.py`, `ui_dummy.py`), but section-specific
backend functionality is intentionally not implemented.
